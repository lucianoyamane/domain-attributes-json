const MENU = 'menu';
const SOURCE = 'source';

const FILE_ID = 'file';
const IS_FILE = 'isFile';

module.exports = {
    MENU,
    SOURCE,
    value: {
        FILE_ID
    },
    attribute: {
        IS_FILE
    }
    
}