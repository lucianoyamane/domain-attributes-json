const MENU = 'menu';
const SOURCE = 'source';

const FILE_ID = 'file';
const IS_FILE = 'isFile';

const PROCESS_KEY = 'process_menu';

module.exports.constants = {
    MENU,
    SOURCE,
    PROCESS_KEY,
    value: {
        FILE_ID
    },
    attribute: {
        IS_FILE
    }
    
}